# `@yoonion/mimi-seed-mcp` — Claude Code context

Package rules are client-neutral, so this file imports the Codex copy instead of duplicating it (one file to
keep true). Repo-wide context stays in the root [`CLAUDE.md`](../../CLAUDE.md).

@AGENTS.md
